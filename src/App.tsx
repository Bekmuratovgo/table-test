import { FC, ReactElement } from 'react';
import TableStyled from './components/Table/Table';

const App: FC = (): ReactElement => {
  return (
    <div className="App">
      <TableStyled />
    </div>
  );
}

export default App;
